// Package auth handles authentication, session lifecycle, and role / permission
// resolution. It exposes the User context and the predicates consumed by
// server/relations/ when deciding which links to emit.
package auth
