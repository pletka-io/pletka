// Package widgets contains the server-side schema builders that produce
// widget JSON from domain objects. One file per widget type. Each builder
// is paired with a Markdown spec under schema/widgets/ and at least one
// fixture under schema/examples/.
package widgets
