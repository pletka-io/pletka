# Pletka

Pletka is a collaborative platform for building semantic data models — "GitHub
for semantic models". Cultural-heritage institutions use it to create, adopt, and
customise the semantic patterns — **fields**, **models**, and **collections** —
that describe their collections, grounded in shared ontologies such as CIDOC-CRM.

See [`docs-oss/VISION.md`](docs-oss/VISION.md) for the product vision.

> **Status:** active development. The Go module is `github.com/pletka-io/pletka`
> and the dev database is `pletka_weave`.

## Architecture

Four ideas carry the codebase:

- **Vertical slices** — each feature is a self-contained slice under
  `pkg/weave/<name>/`: routes, handler, service, store, and schema builders in
  one directory.
- **Schema-driven API** — the backend emits a JSON schema describing structure,
  data, capabilities, and mutation URLs; the frontend renders it generically
  with no domain knowledge.
- **Svelte islands** — interactive components mount into server-rendered Go
  templates. There is no SPA.
- **pgx + sqlc + goose** — the PostgreSQL data layer: typed queries generated by
  sqlc, schema migrations managed by goose and applied at boot.

## Repository layout

```
.
├── cmd/                  Cobra commands: serve, migrate, weave, db, and dev tools
├── pkg/
│   ├── weave/            Vertical slices — one directory per feature
│   ├── formschema/       Schema types + builders (form / list / entity-list)
│   ├── domain/           Domain types, interfaces, Translations
│   ├── database/         goose migrations, sqlc queries + generated code
│   └── assets/           Embedded templates, static dist, configs
├── frontend/             Svelte islands (Vite); builds into pkg/assets/static/dist
├── docs/                 Working docs: specs, schema-ui-core notes
├── docs-oss/             Public docs: vision, ADRs, architecture, reference
└── Makefile              Core build / run / test commands
```

It is a single, self-contained Go module (`github.com/pletka-io/pletka`); the
public/platform split will be carved out later.

## Prerequisites

- Go 1.26+
- PostgreSQL (the dev setup expects it on port 5433)
- Node.js (for the Svelte / Vite frontend)

## Quick start

```bash
cp .env.example .env       # database settings + per-worktree overrides
make frontend-install      # one-time: install frontend dependencies
make air                   # build frontend + run server with hot reload
```

The server starts on port **3333** (`make air PORT=4000` to override). Database
migrations run automatically at boot via goose.

## Common commands

Run `make help` for the full list.

| Command | Purpose |
|---|---|
| `make air` / `make dev` | Run with hot reload |
| `make build` | Build the `pletka` binary into `bin/` |
| `make test` | Run the test suite |
| `make typegen` | Regenerate TypeScript types from Go (tygo) |
| `make frontend-build` | Build the Svelte islands |
| `make db-status` | Show database statistics |
## Configuration

Core server configuration is loaded by the runtime bootstrap from
`configs/config.yaml`, environment variables, and CLI flags. `.env` (gitignored)
holds per-worktree overrides such as `PLETKA_PORT`, `PLETKA_DB_NAME`, and
database credentials.

Customer operations such as Airtable export/import, ontology manifest loading,
full reloads, and deployment belong in the platform repository.

## Documentation

Depth lives in `docs-oss/`, plus in-repo working docs under `docs/`:

- `docs-oss/decisions/` — Architecture Decision Records.
- `docs/specs/` — BDD contracts.
- `docs-oss/architecture/` — active architecture documents.
- `docs-oss/reference/` — Go style, naming conventions, and the CIDOC-CRM
  reference.

New to the codebase? Read [`docs-oss/VISION.md`](docs-oss/VISION.md), then
[`docs-oss/architecture/schema-driven-ui.md`](docs-oss/architecture/schema-driven-ui.md)
and [`docs-oss/architecture/schema-driven-api.md`](docs-oss/architecture/schema-driven-api.md)
to understand how the schema-driven system works.

## License

Licensed under the **Apache License, Version 2.0** — see [`LICENSE`](LICENSE) and
[`NOTICE`](NOTICE). Copyright 2026 Takin.solutions.

```
SPDX-License-Identifier: Apache-2.0
```
