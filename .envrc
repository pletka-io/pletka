# Local direnv defaults for the development worktree.
# Pre-set values win, which keeps alternate worktrees from being
# accidentally forced onto the main local database.
export PLETKA_DB_NAME="${PLETKA_DB_NAME:-pletka_weave}"
export PLETKA_DB_PORT="${PLETKA_DB_PORT:-5433}"
